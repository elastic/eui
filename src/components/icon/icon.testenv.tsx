import React from 'react';
export const EuiIcon = ({ type, ...rest }: any) => (
  <div data-euiicon-type={type} {...rest} />
);

export const TYPES = [];
export const COLORS = [];
export const SIZES = [];
