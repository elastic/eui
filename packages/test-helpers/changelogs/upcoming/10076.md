- Added a `selectors` object that holds every Component Object's selectors keyed by component (`selectors.comboBox.PILL_SELECTOR`)

**Breaking changes**

- Removed the flat `Eui*Selectors` exports. Use the `selectors` object instead.
