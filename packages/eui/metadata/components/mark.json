{
  "name": "EuiMark",
  "description": "Highlights text with a background color (like a highlighter marker).",
  "visualDescription": "Inline text wrapped in a semantic <mark> element with a yellow or highlight-color background that spans just the marked word(s). The surrounding text is unstyled. Visually similar to EuiHighlight — both show highlighted text — but EuiMark wraps static content rather than dynamically matching a search query substring.",
  "category": "Utilities",
  "import": "import { EuiMark } from '@elastic/eui';",
  "props": {
    "aria-label": {
      "type": "string",
      "required": false,
      "description": "Defines a string value that labels the current element.\n@see aria-labelledby."
    },
    "children": {
      "type": "ReactNode",
      "required": false,
      "description": "ReactNode to render as this component's content"
    },
    "hasScreenReaderHelpText": {
      "type": "boolean",
      "required": false,
      "default": "true",
      "description": "Set to `false` to remove the CSS :before and :after\nscreen reader helper text"
    }
  },
  "example": "<EuiMark hasScreenReaderHelpText>Marked text</EuiMark>"
}