**Bug fixes**

- Fixed `EuiValidatableControl` (and any form control using it, e.g. `EuiFieldText`, `EuiTextArea`) showing a native browser validation tooltip on form submit. The `isInvalid` prop no longer hooks into the native constraint validation API (`setCustomValidity`) and instead drives invalid styling purely through `aria-invalid`.
