- `EuiFlyoutMenuAction` accepts any `EuiButtonIcon` prop and arbitrary `data-*` attributes. The menu bar sets `color`, `size`, `display`, `iconSize`, and `isSelected` itself.

**Bug fixes**

- Fixed `EuiFlyoutMenu` actions ignoring `data-test-subj` and other `data-*` attributes.
