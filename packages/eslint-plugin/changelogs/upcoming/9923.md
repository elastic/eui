- Updated `@elastic/eui/no-deprecated-icon-aliases` with the newly deprecated `cloudDrizzle`, `cloudStormy`, `cloudSunny`, `kqlFunction`, `namespace`, `visTimelion`, and `visVisualBuilder` icon aliases

**Breaking changes**

- Removed the `push` icon alias from `@elastic/eui/no-deprecated-icon-aliases`. `push` is now a supported `EuiIcon` glyph again and is no longer autofixed to `send`
