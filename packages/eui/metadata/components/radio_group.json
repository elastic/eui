{
  "name": "EuiRadioGroup",
  "description": "Group of mutually exclusive radio button options.",
  "visualDescription": "A vertical stack of EuiRadio items, each with a small circular radio button on the left and a text label to the right. Items are spaced with consistent vertical gaps. Only one item can be selected at a time (filled circle). NOT EuiCheckboxGroup (circular vs square inputs).",
  "category": "Forms",
  "import": "import { EuiRadioGroup } from '@elastic/eui';",
  "props": {
    "aria-label": {
      "type": "string",
      "required": false,
      "description": "Defines a string value that labels the current element.\n@see aria-labelledby."
    },
    "disabled": {
      "type": "boolean",
      "required": false,
      "description": "Passed down to all child `EuiCheckbox`es"
    },
    "compressed": {
      "type": "boolean",
      "required": false,
      "description": "Tightens up the spacing between radio rows"
    },
    "name": {
      "type": "string",
      "required": false
    },
    "options": {
      "type": "EuiRadioGroupOption[]",
      "required": false,
      "default": "[]"
    },
    "idSelected": {
      "type": "string",
      "required": false
    },
    "onChange": {
      "type": "EuiRadioGroupChangeCallback",
      "required": true
    },
    "legend": {
      "type": "HTMLAttributes<HTMLLegendElement> & CommonProps & { children: ReactNode; display?: \"hidden\" | \"visible\"; compressed?: boolean; }",
      "required": false,
      "description": "Adds an EuiFormLegend element as the first child\nIf the individual labels for each radio do not provide a sufficient description, add a legend.\nWraps the group in a `EuiFormFieldset` which adds an `EuiLegend` for titling the whole group.\nAccepts an `EuiFormLegendProps` shape."
    }
  },
  "example": "<EuiRadioGroup id=\"radio-1\" label=\"Radio 1\" value=\"radio-1\" />"
}