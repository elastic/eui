import React from 'react';

import {
  EuiFilePicker,
} from '../../../../src/components';

export default () => (
  <div>
    <EuiFilePicker multiple initialButtonText="Choose multiple files" />
  </div>
);
