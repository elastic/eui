- Added `controls` and `flask` glyphs to `EuiIcon`

**Breaking changes**

- Removed `beaker`, `controlsHorizontal`, `controlsVertical`, `editorCodeBlock`, `userAvatar`, `visVega` glyphs from `EuiIcon`
