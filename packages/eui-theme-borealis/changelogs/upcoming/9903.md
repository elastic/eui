- Added `colors.backgroundBaseCanvas` for nested workspace surfaces (light `#F8F9FB` via `shade8`, dark `#0B0F17`); kept separate from app chrome
- Light app chrome (`colors.body` / `$euiPageBackgroundColor`) remains `#EAEDF5` (`shade17`)
- Updated `colors.backgroundBaseSubdued` (light `#F8F9FB`, dark `#14171D`) and dark `backgroundBasePlain` to `#161B22`
- Added `border.radius.inline` (4px), `border.radius.control` (8px), `border.radius.panel` (12px), and `border.radius.container` (16px); legacy `small`/`medium` remain 4px
