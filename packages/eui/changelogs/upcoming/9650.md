**Bug fixes**

- Fixed `EuiCopy` not showing the `afterMessage` tooltip after a successful copy when the user had already hovered or focused the trigger before clicking

**Accessibility**

- Added a screen reader live region to `EuiCopy` so the copy confirmation is announced reliably, independent of the tooltip and focus location
