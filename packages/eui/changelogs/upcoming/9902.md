- Changed default `EuiBasicTable` / `EuiInMemoryTable` item action color from `primary` to `text` when `color` is omitted
- Changed `EuiDataGrid` cell action and expand icons from primary/fill to text/empty for quieter row chrome
