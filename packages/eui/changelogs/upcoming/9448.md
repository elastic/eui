- Added `color` prop to `EuiContextMenuItem`, accepting all standard button color values

**Bug fixes**

- Fixed `DefaultItemAction.color` being ignored in the collapsed overflow menu (`...` popover) of `EuiBasicTable` — it now renders with the correct color, matching the expanded inline action view
