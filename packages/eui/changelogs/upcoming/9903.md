- Added `colors.backgroundBaseCanvas` for page/canvas chrome and aligned deprecated `colors.body` to it (light `#F8F9FB` via `shade8`, dark `#0B0F17`)
- Updated `colors.backgroundBaseSubdued` for secondary/inner surfaces (light `#F8F9FB`, dark `#14171D`) and dark `backgroundBasePlain` to `#161B22`
- Added `border.radius.inline` (4px), `border.radius.control` (8px), `border.radius.panel` (12px), and `border.radius.container` (16px); legacy `small`/`medium` remain 4px
- Migrated `EuiSkeleton*`, `EuiColorPickerSwatch`, and `EuiCode` to `border.radius.inline`
