- Updated `EuiSuperDatePicker` to expose plain text `utcOffset` and `timeZoneName` in `timeZoneDisplayProps.customRender` render function

**Bug fixes**

- Fixed icon size in `EuiSuperDatePicker`'s time window buttons when `compressed={true}`

