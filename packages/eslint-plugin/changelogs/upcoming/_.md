- Added new `@elastic/eui/copy-component-rules` rule that flags `EuiCopy` using `beforeMessage` when its render-prop child is an `EuiToolTip`, and recommends removing the `EuiToolTip` wrapper and moving its message into the `beforeMessage` prop
- Updated `@elastic/eui/tooltip-button-icon-wrap` to no longer require an `EuiToolTip` wrapper for an `EuiButtonIcon` rendered inside an `EuiCopy`, since `EuiCopy` already provides a tooltip via `beforeMessage`
