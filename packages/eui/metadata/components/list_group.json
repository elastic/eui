{
  "name": "EuiListGroup",
  "description": "Vertical list of action items or links.",
  "category": "Display",
  "import": "import { EuiListGroup } from '@elastic/eui';",
  "props": {
    "aria-label": {
      "type": "string",
      "required": false,
      "description": "Defines a string value that labels the current element.\n@see aria-labelledby."
    },
    "bordered": {
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "Add a border to the list container"
    },
    "flush": {
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "Remove container padding, stretching list items to the edges"
    },
    "gutterSize": {
      "type": "\"s\" | \"m\" | \"none\"",
      "required": false,
      "default": "s",
      "description": "Spacing between list items"
    },
    "listItems": {
      "type": "EuiListGroupItemProps[]",
      "required": false,
      "description": "Items to display in this group. See {@link EuiListGroupItem}"
    },
    "color": {
      "type": "\"text\" | \"subdued\" | \"primary\"",
      "required": false,
      "default": "text",
      "description": "Change the colors of all `listItems` at once"
    },
    "size": {
      "type": "\"s\" | \"xs\" | \"m\" | \"l\"",
      "required": false,
      "default": "m",
      "description": "Change the size of all `listItems` at once"
    },
    "maxWidth": {
      "type": "boolean | MaxWidth<string | number>",
      "required": false,
      "default": "true",
      "description": "Sets the max-width of the page.\nSet to `true` to use the default size,\nset to `false` to not restrict the width,\nor set to a number/string for a custom CSS width/measurement."
    },
    "showToolTips": {
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "Display tooltips on all list items"
    },
    "wrapText": {
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "Allow link text to wrap vs truncated"
    },
    "ariaLabelledby": {
      "type": "string",
      "required": false
    }
  },
  "example": "<EuiListGroup />"
}