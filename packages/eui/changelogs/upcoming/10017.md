- Updated `EuiBasicTable` and `EuiInMemoryTable` to use `border.radius.panel` when `panelled={true}`
- Updated `EuiTable` to use `border.radius.panel` for mobile panels
- Updated `EuiTable` and `EuiDataGrid` cell actions to `color="text"`

**Bug fixes**

- Fixed a visual bug on `EuiBasicTable` with `scrollInline={true}` and `stickyHeader={true}` where header cells weren't properly aligned if they contain a tooltip