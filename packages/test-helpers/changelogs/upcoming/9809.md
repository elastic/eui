- Added `EuiComboBoxObject.setCustomSelectedOptions()` and `getAllVisibleOptions()`
- Added a `timeout` option to `EuiComboBoxObject.setSelectedOptions()`
- Updated `EuiComboBoxObject.setSelectedOptions()` to type-to-filter, so it works on filterable / virtualized / async combo boxes
- Updated Component Objects to verify the target element's component type and throw on mismatch

**Bug fixes**

- Fixed `EuiComboBoxObject.getSelectedOptions()` to read selected pills by class, so combo boxes that set a per-option `data-test-subj` are read correctly
