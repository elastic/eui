- Bundled raw icon SVG files into `dist/svgs/` and a `dist/eui-icons.json` manifest (mapping icon filename stems to SVG content strings) to allow consumers to access and rasterize EUI icons without going through the React component layer
- Copied icon SVG source files to all build outputs (`es/`, `lib/`, `optimize/es/`, `optimize/lib/`)
