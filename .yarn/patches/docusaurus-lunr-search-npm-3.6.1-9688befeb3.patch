diff --git a/src/theme/SearchBar/lunar-search.js b/src/theme/SearchBar/lunar-search.js
index 241930959ec9813b6a9b93a29b431a03c7fd784c..2e9412e4450a984d295527f410bb9f048b34fc1e 100644
--- a/src/theme/SearchBar/lunar-search.js
+++ b/src/theme/SearchBar/lunar-search.js
@@ -29,10 +29,31 @@ class LunrSearchAdapter {
     }
 
     getHit(doc, formattedTitle, formattedContent) {
+        // Extract site section from URL for better context
+        const getSectionFromUrl = (url) => {
+            const urlParts = url.split('/');
+            const docsIndex = urlParts.indexOf('docs');
+            if (docsIndex !== -1 && urlParts.length > docsIndex + 1) {
+                const sectionSlug = urlParts[docsIndex + 1];
+                const sectionMap = {
+                    'getting-started': 'Getting started',
+                    'components': 'Components',
+                    'utilities': 'Utilities',
+                    'patterns': 'Patterns',
+                    'content': 'Content',
+                    'dataviz': 'Data visualization'
+                };
+                return sectionMap[sectionSlug] || sectionSlug;
+            }
+            return null;
+        };
+
+        const siteSection = getSectionFromUrl(doc.url);
+        
         return {
             hierarchy: {
-                lvl0: doc.pageTitle || doc.title,
-                lvl1: doc.type === 0 ? null : doc.title
+                lvl0: siteSection || (doc.type === 0 ? doc.title : doc.pageTitle),
+                lvl1: doc.type === 0 ? null : (siteSection ? doc.pageTitle : doc.heading)
             },
             url: doc.url,
             version: doc.version,
@@ -45,13 +66,13 @@ class LunrSearchAdapter {
             _highlightResult: {
                 hierarchy: {
                     lvl0: {
-                        value: doc.type === 0 ? formattedTitle || doc.title : doc.pageTitle,
+                        value: siteSection || (doc.type === 0 ? formattedTitle || doc.title : doc.pageTitle),
                     },
                     lvl1:
                         doc.type === 0
                             ? null
                             : {
-                                value: formattedTitle || doc.title
+                                value: siteSection ? (formattedTitle || doc.pageTitle) : (formattedTitle || doc.heading)
                             }
                 }
             }
@@ -60,14 +81,16 @@ class LunrSearchAdapter {
     getTitleHit(doc, position, length) {
         const start = position[0];
         const end = position[0] + length;
-        let formattedTitle = doc.title.substring(0, start) + '<span class="algolia-docsearch-suggestion--highlight">' + doc.title.substring(start, end) + '</span>' + doc.title.substring(end, doc.title.length);
+        const titleText = doc.type === 0 ? doc.title : doc.heading;
+        let formattedTitle = titleText.substring(0, start) + '<span class="algolia-docsearch-suggestion--highlight">' + titleText.substring(start, end) + '</span>' + titleText.substring(end, titleText.length);
         return this.getHit(doc, formattedTitle)
     }
 
     getKeywordHit(doc, position, length) {
         const start = position[0];
         const end = position[0] + length;
-        let formattedTitle = doc.title + '<br /><i>Keywords: ' + doc.keywords.substring(0, start) + '<span class="algolia-docsearch-suggestion--highlight">' + doc.keywords.substring(start, end) + '</span>' + doc.keywords.substring(end, doc.keywords.length) + '</i>'
+        const titleText = doc.type === 0 ? doc.title : doc.heading;
+        let formattedTitle = titleText + '<br /><i>Keywords: ' + doc.keywords.substring(0, start) + '<span class="algolia-docsearch-suggestion--highlight">' + doc.keywords.substring(start, end) + '</span>' + doc.keywords.substring(end, doc.keywords.length) + '</i>'
         return this.getHit(doc, formattedTitle)
     }
 
