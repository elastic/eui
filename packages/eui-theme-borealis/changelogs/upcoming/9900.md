- Softened `colors.textParagraph` and legacy `colors.text`: light `shade110` (`#384861`), dark `shade40` (`#B4C1D5`). `textSubdued` and `textHeading` are unchanged

**Breaking changes**

- Default paragraph / body text contrast is slightly lower. Prefer `textHeading` or an explicit color where full emphasis is required
