- In `getDefaultEuiMarkdownPlugins`, adds an `allowBareRelative` option to `parsingConfig`, allowing the Markdown editor to parse bare relative links such as `[link](page)`, which would resolve to something like `https://current.base/url/page`. This replicates the way a browser would natively resolve this kind of href without having to bypass the link validator.
