{
  "name": "EuiConfirmModal",
  "description": "Standardised confirmation dialog for destructive or irreversible actions.",
  "visualDescription": "A centered modal dialog with a dark full-viewport overlay. Contains: a header with a bold title and an × close button, a body with a short descriptive text paragraph, and a footer with exactly two buttons — a Cancel (empty/outlined) button and a primary action button (often red for destructive actions). Narrower than a general-purpose modal. NOT a multi-step wizard modal (only two footer buttons). NOT EuiFlyout.",
  "category": "Layout",
  "import": "import { EuiConfirmModal } from '@elastic/eui';",
  "props": {
    "children": {
      "type": "ReactNode",
      "required": false,
      "description": "ReactNode to render as this component's content"
    },
    "title": {
      "type": "ReactNode",
      "required": false
    },
    "titleProps": {
      "type": "Omit<EuiTitleProps, \"children\"> & HTMLAttributes<HTMLHeadingElement> & { component?: ElementType; }",
      "required": false
    },
    "cancelButtonText": {
      "type": "ReactNode",
      "required": false
    },
    "confirmButtonText": {
      "type": "ReactNode",
      "required": false
    },
    "onCancel": {
      "type": "(event?: any) => void",
      "required": true
    },
    "onConfirm": {
      "type": "(event: React.MouseEvent<HTMLButtonElement, MouseEvent>) => void",
      "required": false
    },
    "confirmButtonDisabled": {
      "type": "boolean",
      "required": false
    },
    "defaultFocusedButton": {
      "type": "\"cancel\" | \"confirm\"",
      "required": false,
      "description": "Allows focusing either the confirm or cancel button on modal initialization.\nWill take precedence over `initialFocus`, if `initialFocus` is passed."
    },
    "buttonColor": {
      "type": "\"text\" | \"accent\" | \"accentSecondary\" | \"primary\" | \"success\" | \"warning\" | \"danger\" | \"neutral\" | \"risk\"",
      "required": false,
      "default": "primary"
    },
    "maxWidth": {
      "type": "string | number | boolean",
      "required": false,
      "description": "Sets the max-width of the modal.\nSet to `true` to use the default (`euiBreakpoints 'm'`),\nset to `false` to not restrict the width,\nset to a number for a custom width in px,\nset to a string for a custom width in custom measurement."
    },
    "isLoading": {
      "type": "boolean",
      "required": false,
      "description": "Passes `isLoading` prop to the confirm button"
    },
    "role": {
      "type": "\"dialog\" | \"alertdialog\"",
      "required": false,
      "description": "Identifies a modal dialog to screen readers. Modal dialogs that confirm destructive actions\nor need a user's attention should use \"alertdialog\"."
    },
    "aria-label": {
      "type": "string",
      "required": false,
      "description": "Defines a string value that labels the current element.\n@see aria-labelledby."
    },
    "outsideClickCloses": {
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "Whether clicking outside the modal should close it."
    },
    "focusTrapProps": {
      "type": "Pick<EuiFocusTrapProps, \"returnFocus\">",
      "required": false,
      "description": "Object of props passed to EuiFocusTrap.\n`returnFocus` defines the return focus behavior and provides the possibility to check the available target element or opt out of the behavior in favor of manually returning focus"
    },
    "initialFocus": {
      "type": "string | HTMLElement | (() => HTMLElement)",
      "required": false,
      "description": "Specifies what element should initially have focus.\nCan be a DOM node, or a selector string (which will be passed to document.querySelector() to find the DOM node), or a function that returns a DOM node."
    }
  },
  "example": "<EuiConfirmModal title=\"Confirm modal title\" confirmButtonText=\"Confirm\" cancelButtonText=\"Cancel\">Confirm modal content</EuiConfirmModal>"
}