# Panel

**EuiPanel** is a building block component. Use it as a layout helper for containing content. It is also commonly used as a base for other larger components like [EuiPage](../../templates/page-template/index.mdx), [EuiPopover](../popover.mdx) and [EuiCard](../card.mdx).

## Component

### Default

Panels have several possible style combinations with color, borders, and shadow. Use them sparingly and favor the default.

```tsx interactive
import React from 'react';
import { EuiPanel } from '@elastic/eui';

export default () => (
  <EuiPanel>
    Default panel
  </EuiPanel>
);
```

### Split

import SplitPanel from './_split_panel.mdx';

<SplitPanel />

## Usage

### Padding

The most basic use is to simply add padding around your content.

```tsx interactive
import React from 'react';
import { EuiPanel, EuiCode, EuiSpacer } from '@elastic/eui';

export default () => (
  <div>
    <EuiPanel paddingSize="none">
      <EuiCode>paddingSize=&quot;none&quot;</EuiCode>
    </EuiPanel>

    <EuiSpacer size="l" />

    <EuiPanel paddingSize="s">
      <EuiCode>paddingSize=&quot;s&quot;</EuiCode>
    </EuiPanel>

    <EuiSpacer size="l" />

    <EuiPanel paddingSize="m">
      <EuiCode>paddingSize=&quot;m&quot; (default)</EuiCode>
    </EuiPanel>

    <EuiSpacer size="l" />

    <EuiPanel paddingSize="l">
      <EuiCode>paddingSize=&quot;l&quot;</EuiCode>
    </EuiPanel>
  </div>
);
```

### Shadow and border

**EuiPanel** can give depth to your container with `hasShadow` while `hasBorder` can add containment. Just be sure not to include too many [nested panels](#guidelines with these settings.

:::warning Certain allowed combinations of shadow, border, and color depend on the current theme.

For instance, only plain or transparent panels can have a border and/or shadow. The default theme doesn't allow combining the `hasBorder` option with `hasShadow`.

:::

```tsx interactive
import React from 'react';
import { EuiPanel, EuiCode, EuiSpacer } from '@elastic/eui';

export default () => {
  return (
    <div>
      <EuiPanel hasShadow={false}>
        <EuiCode>{'hasShadow={false}'}</EuiCode>
      </EuiPanel>

      <EuiSpacer />

      <EuiPanel hasBorder={true}>
        <EuiCode>{'hasBorder={true}'}</EuiCode>
      </EuiPanel>
    </div>
  );
};
```

### Colors and corners

Use `color` to add background shading to your panel and provide an additional helpful aesthetic to your container in context. Be mindful to use color sparingly. You can also remove the rounded corners depending on the placement of your panel with `borderRadius="none"`

Passing `color="transparent"` can give you a quick empty box simply for adding padding to all sides.

```tsx interactive
import React from 'react';
import { EuiPanel, EuiSpacer } from '@elastic/eui';

export default () => (
  <div>
    <EuiPanel color="subdued" borderRadius="none" hasShadow={false}>
      <p>I am a simple shaded box</p>
    </EuiPanel>

    <EuiSpacer />

    <EuiPanel color="transparent" hasBorder={false}>
      <p>I am a transparent box simply for padding</p>
    </EuiPanel>
  </div>
);
```

### Growing height

Using **EuiPanel** in an [EuiFlexItem](../../layout/flex/item.mdx#panels-grow-vertically-to-fill-flex-items) will always grow its height to match. This is great for rows of panels. However, you can also turn this feature off by setting `grow={false}`.

```tsx interactive
import React from 'react';
import { EuiPanel, EuiFlexGroup, EuiFlexItem, EuiText } from '@elastic/eui';

export default () => (
  <EuiFlexGroup gutterSize="l">
    <EuiFlexItem>
      <EuiPanel>
        <EuiText>
          <p>
            I am some panel content...
            <br /> ... <br /> ... <br />
            whose content is tall
          </p>
        </EuiText>
      </EuiPanel>
    </EuiFlexItem>
    <EuiFlexItem>
      <EuiPanel grow={true}>
        <EuiText>
          <p>I am some panel content... whose content will grow</p>
        </EuiText>
      </EuiPanel>
    </EuiFlexItem>
    <EuiFlexItem>
      <EuiPanel grow={false}>
        <EuiText>
          <p>I am some panel content... whose content did not grow</p>
        </EuiText>
      </EuiPanel>
    </EuiFlexItem>
  </EuiFlexGroup>
);
```

## Guidelines

import Guidelines from './_guidelines.mdx';

<Guidelines />

## Props

import docgen from '@elastic/eui-docgen/dist/components/panel';

<PropTable definition={docgen.EuiPanel} />

import PropTableSplitPanel from './_split_panel_props';

<PropTableSplitPanel />
