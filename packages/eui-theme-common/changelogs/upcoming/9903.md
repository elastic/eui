- Added `colors.backgroundBaseCanvas` type (nested workspace surfaces; not app chrome)
- Clarified deprecated `colors.body` docs vs canvas
- Added `border.radius.inline`, `border.radius.control`, `border.radius.panel`, and `border.radius.container` types
