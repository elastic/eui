**Bug fixes**

- Fixed `EuiDescriptionListTitle` and `EuiDescriptionListDescription` so the `compressed` prop correctly applies a compressed font size for every `textStyle` (`normal` and `reverse`) permutation, instead of only one combination

