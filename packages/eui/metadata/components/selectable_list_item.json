{
  "name": "EuiSelectableListItem",
  "description": "",
  "visualDescription": "",
  "category": "Forms",
  "import": "import { EuiSelectableListItem } from '@elastic/eui';",
  "props": {
    "aria-label": {
      "type": "string",
      "required": false,
      "description": "Defines a string value that labels the current element.\n@see aria-labelledby."
    },
    "checked": {
      "type": "any",
      "required": false,
      "description": "Applies an icon and visual styling to activated items"
    },
    "showIcons": {
      "type": "boolean",
      "required": false,
      "default": "true",
      "description": "Shows icons based on `checked` type"
    },
    "isFocused": {
      "type": "boolean",
      "required": false,
      "description": "Highlights the item for pseudo focus"
    },
    "disabled": {
      "type": "boolean",
      "required": false
    },
    "prepend": {
      "type": "React.ReactNode",
      "required": false
    },
    "append": {
      "type": "React.ReactNode",
      "required": false
    },
    "allowExclusions": {
      "type": "boolean",
      "required": false
    },
    "onFocusBadge": {
      "type": "boolean | EuiBadgeProps",
      "required": false,
      "default": "true",
      "description": "When enabled by setting to either `true` or passing custom a custom badge,\nshows a hollow badge as an append (far right) when the item is focused.\nThe default content when `true` is `↩ to select/deselect/include/exclude`"
    },
    "paddingSize": {
      "type": "\"s\" | \"none\"",
      "required": false,
      "default": "s",
      "description": "Padding for the list items."
    },
    "searchable": {
      "type": "boolean",
      "required": false,
      "description": "Whether the `EuiSelectable` instance is searchable.\nWhen true, the Space key will not toggle selection, as it will type into the search box instead. Screen reader instructions will be added instructing users to use the Enter key to select items.\nWhen false, the Space key will toggle item selection. No extra screen reader instructions will be added, as Space to toggle is a generally standard for most select/checked elements."
    },
    "textWrap": {
      "type": "\"wrap\" | \"truncate\"",
      "required": false,
      "default": "truncate",
      "description": "How to handle long text within the item.\nWrapping only works if virtualization is off."
    },
    "toolTipContent": {
      "type": "ReactNode",
      "required": false,
      "description": "Optional custom tooltip content for the button"
    },
    "toolTipProps": {
      "type": "Partial<Omit<EuiToolTipProps, \"children\" | \"content\">>",
      "required": false,
      "description": "Optional props to pass to the underlying **[EuiToolTip](/#/display/tooltip)**"
    }
  },
  "example": "<EuiSelectableListItem>Selectable list item</EuiSelectableListItem>"
}