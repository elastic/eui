**Bug fixes**

- Fixed `EuiSelectable` to preserve the active option's focus state when switching focus between the search input and the options listbox

**Accessibility**

- Improved the accessibility of `EuiSelectable` by removing empty `aria-activedescendant` attribute when no option is active

