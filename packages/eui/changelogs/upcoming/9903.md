- Added `colors.backgroundBaseCanvas` for nested workspace surfaces (light `#F8F9FB` via `shade8`, dark `#07101F` via `plainDark`); kept separate from app chrome
- Light app chrome (`colors.body` / `$euiPageBackgroundColor`) remains `#EAEDF5` (`shade17`); do not alias to canvas
- Synced Borealis theme JSON vars for Kibana `getEuiThemeVars`: page/canvas/subdued/plain, radius tokens (`inline`/`control`/`panel`/`container`), form/panel radius Sass mirrors, quieter `textParagraph`
- Updated `colors.backgroundBaseSubdued` for secondary/inner surfaces (light `#F8F9FB`, dark `#0B1628` via `shade145`) and dark `backgroundBasePlain` to `#111C2C` (`shade140`)
- Added `border.radius.inline` (4px), `border.radius.control` (8px), `border.radius.panel` (12px), and `border.radius.container` (16px); legacy `small`/`medium` remain 4px
- Migrated `EuiSkeleton*`, `EuiColorPickerSwatch`, and `EuiCode` to `border.radius.inline`
