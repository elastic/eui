- Removed inline padding from `EuiTab` so tab labels sit flush with the edge
- Updated `EuiTab` text colors so inactive tabs use subdued text and the selected tab uses heading text; the selected underline remains primary
