- Extended `bottomBorder` prop to all `'extended'` on `EuiPageHeader`
- Added `paddingSize` and `restrictWidth` directly on `EuiPageHeaderContent`

**Bug fixes**

- Fixed missing render of `breadcrumbs` on `EuiPageHeader` when `alignItems = 'top'`
