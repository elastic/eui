- Added `colors.backgroundBaseRecessed` as an intermediate, seldom-used layer (e.g. dashboard / Workflows wrappers; light `#F6F9FC` via `shade10`, dark `#07101F` via `plainDark`)
- Light app chrome (`colors.body` / `$euiPageBackgroundColor`) uses `#ECF1F9` (`shade15`)
- Synced Borealis theme JSON vars for Kibana `getEuiThemeVars`: page/recessed/subdued/plain, radius tokens (`inline`/`control`/`panel`/`frame`), form/panel radius Sass mirrors, quieter `textParagraph`
- Updated `colors.backgroundBaseSubdued` (light `#F6F9FC` via `shade10`, dark `#0B1628` via `shade145`) and dark `backgroundBasePlain` to `#111C2C` (`shade140`)
- Added `border.radius.inline` (4px), `border.radius.control` (8px), `border.radius.panel` (12px), and `border.radius.frame` (16px); legacy `small`/`medium` remain 4px
