{
  "name": "EuiTab",
  "description": "Individual tab item. Must be a child of EuiTabs.",
  "visualDescription": "A single tab item — a text label with generous top/bottom padding and no visible outer border. When active, shows a solid 2–3px underline in the primary color directly below the label. When inactive, the label is subdued. Part of an EuiTabs row. NOT a button (no bounding box visible at rest).",
  "category": "Navigation",
  "import": "import { EuiTab } from '@elastic/eui';",
  "props": {
    "href": {
      "type": "string",
      "required": false
    },
    "isSelected": {
      "type": "boolean",
      "required": false
    },
    "disabled": {
      "type": "boolean",
      "required": false
    },
    "prepend": {
      "type": "ReactNode",
      "required": false,
      "description": "Places content before the tab content/children.\nWill be excluded from interactive effects."
    },
    "append": {
      "type": "ReactNode",
      "required": false,
      "description": "Places content after the tab content/children.\nWill be excluded from interactive effects."
    },
    "aria-label": {
      "type": "string",
      "required": false,
      "description": "Defines a string value that labels the current element.\n@see aria-labelledby."
    },
    "onClick": {
      "type": "MouseEventHandler<HTMLAnchorElement> | MouseEventHandler<HTMLButtonElement>",
      "required": false
    }
  },
  "example": "<EuiTab>Tab label</EuiTab>"
}