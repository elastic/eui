- Deprecated additional `EuiIcon` types that are not represented in Figma

**Breaking changes**

- Removed most deprecated `EuiIcon` types and their icon assets. 16 widely-used deprecated aliases (e.g. `alert`, `search`, `visPie`) remain temporarily and will be removed in a future release ([#9832](https://github.com/elastic/eui/issues/9832))
