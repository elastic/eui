**Bug fixes**

- Fixed `EuiFlyout` leaving stale push padding on the page when multiple `type="push"` flyouts share a padding target (a `container` or `document.body`) — closing them could strand the offset (page still pushed with no flyout open), and across separate React roots stacked flyouts could alternate between push and overlay
