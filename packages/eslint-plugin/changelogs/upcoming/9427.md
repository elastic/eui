- Added `EuiPopover` and `EuiWrappingPopover` checks to `require-aria-label-for-modals`, requiring either `aria-label` or `aria-labelledby` for these popover components.
- Updated `require-aria-label-for-modals` to skip ARIA validation when the component has spread attributes (for example, `<EuiModal {...props} />`).
