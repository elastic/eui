- Decreased `EuiTitle` letter-spacing via `font.title.letterSpacing` (`-0.2px`)
- Softened Borealis `colors.textParagraph` (and legacy `colors.text`) toward subdued: light `shade110`, dark `shade40`. Root/`html` and inheriting components (including bare `EuiText`) pick this up; `textSubdued` and `textHeading` are unchanged
- Changed default `EuiText` `size` to `s` to match theme body / unstyled `html`; pass `size="m"` for the previous ~16px default

**Breaking changes**

- Default body / paragraph text is slightly quieter. Use `textHeading` or a custom color where stronger contrast is required
- Default `EuiText` `size` is smaller when omitted; pass `size="m"` where the previous appearance is required
