**Bug fixes**

- Fixed `EuiFlyout` clamping a newly opened main flyout's width against the previously open main flyout, which made alternate opens come out at different widths. The sibling used for the resizable width clamp is now derived from the flyout's own session.
