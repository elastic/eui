- Updated `EuiAvatar` to support a new letter `casing` prop that allow customizing text capitalization

**Breaking changes**

- `EuiAvatar`s with the default `user` type will now default to capitalizing all initials in uppercase
