{
  "name": "EuiModal",
  "description": "Focused dialog that traps focus and requires explicit user action.",
  "visualDescription": "A centered dialog box that floats above the page behind a full-viewport dark overlay/backdrop. Fixed width, variable height. Structured in three regions: a header bar (title left, × close button right), a scrollable body, and a footer with action buttons (typically Cancel + primary action). Corners are rounded. NOT EuiFlyout (centered vs edge-anchored). EuiConfirmModal is a pre-structured variant with a title, body text, and exactly two buttons.",
  "category": "Layout",
  "import": "import { EuiModal } from '@elastic/eui';",
  "props": {
    "children": {
      "type": "ReactNode",
      "required": true,
      "description": "ReactNode to render as this component's content"
    },
    "onClose": {
      "type": "(event?: any) => void",
      "required": true
    },
    "maxWidth": {
      "type": "string | number | boolean",
      "required": false,
      "default": "true",
      "description": "Sets the max-width of the modal.\nSet to `true` to use the default (`euiBreakpoints 'm'`),\nset to `false` to not restrict the width,\nset to a number for a custom width in px,\nset to a string for a custom width in custom measurement."
    },
    "initialFocus": {
      "type": "string | HTMLElement | (() => HTMLElement)",
      "required": false,
      "description": "Specifies what element should initially have focus.\nCan be a DOM node, or a selector string (which will be passed to document.querySelector() to find the DOM node), or a function that returns a DOM node."
    },
    "role": {
      "type": "\"dialog\" | \"alertdialog\"",
      "required": false,
      "default": "dialog",
      "description": "Identifies a modal dialog to screen readers. Modal dialogs that confirm destructive actions\nor need a user's attention should use \"alertdialog\"."
    },
    "focusTrapProps": {
      "type": "Pick<EuiFocusTrapProps, \"returnFocus\">",
      "required": false,
      "description": "Object of props passed to EuiFocusTrap.\n`returnFocus` defines the return focus behavior and provides the possibility to check the available target element or opt out of the behavior in favor of manually returning focus"
    },
    "outsideClickCloses": {
      "type": "boolean",
      "required": false,
      "default": "false",
      "description": "Whether clicking outside the modal should close it."
    },
    "aria-label": {
      "type": "string",
      "required": false,
      "description": "Defines a string value that labels the current element.\n@see aria-labelledby."
    }
  },
  "example": "<EuiModal role=\"dialog\" maxWidth />"
}