
describe.skip('Barchart general tests', () => {
  it('should render custom xDomain', () => {
    expect(false).toBe(true);
  });

  it('should render custom yDomain', () => {
    expect(false).toBe(true);
  });

  it('should render custom domains', () => {
    expect(false).toBe(true);
  });

  it('should not render elements not in custom domain', () => {
    expect(false).toBe(true);
  });

  it('should render negative stacked data', () => {
    expect(false).toBe(true);
  });
  // add a test to check the clipping of elements (if possible)
});
