**Breaking changes**

- Added a new `logoType` prop to `EuiHeaderLogo` to render either the `glyph` or `horizontal` Elastic logo. The `iconType` prop has been removed, and `children` text content is no longer rendered.